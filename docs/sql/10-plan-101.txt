-- Query plans for Trends.MeterMonthVE, PostgreSQL 16, 6570 readings / 6 meters.
-- EXPLAIN (ANALYZE, COSTS OFF, TIMING OFF); statements in 09-filter-pushdown-101.sql.

--- (a) no filter: every reading is aggregated
                                                        QUERY PLAN                                                         
---------------------------------------------------------------------------------------------------------------------------
 Limit (actual rows=216 loops=1)
   ->  HashAggregate (actual rows=216 loops=1)
         Group Key: m.metercode, m.region, (EXTRACT(year FROM r.readat))::integer, (EXTRACT(month FROM r.readat))::integer
         Batches: 1  Memory Usage: 337kB
         ->  Hash Join (actual rows=6570 loops=1)
               Hash Cond: (r."trends$reading_meter" = m.id)
               ->  Seq Scan on "trends$reading" r (actual rows=6570 loops=1)
               ->  Hash (actual rows=6 loops=1)
                     Buckets: 1024  Batches: 1  Memory Usage: 9kB
                     ->  Seq Scan on "trends$meter" m (actual rows=6 loops=1)
 Planning Time: 0.905 ms
 Execution Time: 6.043 ms
(12 rows)


--- (b) the same view, filtered on one meter and one year
                                                    QUERY PLAN                                                     
-------------------------------------------------------------------------------------------------------------------
 Limit (actual rows=12 loops=1)
   ->  Sort (actual rows=12 loops=1)
         Sort Key: ((EXTRACT(month FROM r.readat))::integer)
         Sort Method: quicksort  Memory: 25kB
         ->  GroupAggregate (actual rows=12 loops=1)
               Group Key: m.region, ((EXTRACT(month FROM r.readat))::integer)
               ->  Sort (actual rows=364 loops=1)
                     Sort Key: m.region, ((EXTRACT(month FROM r.readat))::integer)
                     Sort Method: quicksort  Memory: 50kB
                     ->  Nested Loop (actual rows=364 loops=1)
                           Join Filter: (r."trends$reading_meter" = m.id)
                           Rows Removed by Join Filter: 1820
                           ->  Bitmap Heap Scan on "trends$meter" m (actual rows=1 loops=1)
                                 Recheck Cond: ((metercode)::text = 'M-004'::text)
                                 Heap Blocks: exact=1
                                 ->  Bitmap Index Scan on "idx_trends$meter_metercode_asc" (actual rows=1 loops=1)
                                       Index Cond: ((metercode)::text = 'M-004'::text)
                           ->  Materialize (actual rows=2184 loops=1)
                                 ->  Seq Scan on "trends$reading" r (actual rows=2184 loops=1)
                                       Filter: ((EXTRACT(year FROM readat))::integer = 2025)
                                       Rows Removed by Filter: 4386
 Planning Time: 0.226 ms
 Execution Time: 2.745 ms
(23 rows)

